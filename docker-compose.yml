services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: solicitor
      POSTGRES_PASSWORD: solicitor
      POSTGRES_DB: solicitor_scraper
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
